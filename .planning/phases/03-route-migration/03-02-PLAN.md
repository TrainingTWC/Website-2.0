---
phase: 03-route-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/MorphingHeader.tsx
  - app/products/[slug]/page.tsx
  - app/products/[slug]/ProductClient.tsx
  - app/journal/page.tsx
  - app/journal/[id]/page.tsx
  - app/journal/[id]/PostDetailClient.tsx
  - app/orders/[id]/page.tsx
  - app/orders/[id]/OrderConfirmationClient.tsx
autonomous: true
requirements:
  - ROUTE-03
  - ROUTE-04
  - ROUTE-05
  - ROUTE-07

must_haves:
  truths:
    - "MorphingHeader is a standalone exported component in src/components/MorphingHeader.tsx"
    - "Visiting /products/[slug] renders ProductPage for the given product _id"
    - "generateStaticParams() for /products/[slug] fetches all product _ids from Convex HTTP API at build time"
    - "Visiting /journal renders EditorialHub with router-based navigation"
    - "Visiting /journal/[id] renders PostDetail for the given post ID"
    - "Visiting /orders/[id] renders OrderConfirmation for the given order ID"
  artifacts:
    - path: "src/components/MorphingHeader.tsx"
      provides: "Extracted MorphingHeader component + useActiveSection hook"
      exports: ["MorphingHeader", "useActiveSection"]
    - path: "app/products/[slug]/page.tsx"
      provides: "Server module: generateStaticParams + server component shell"
    - path: "app/products/[slug]/ProductClient.tsx"
      provides: "Client component: renders ProductPage with context hooks"
      exports: ["ProductClient"]
    - path: "app/journal/page.tsx"
      provides: "App Router page for /journal"
      exports: ["default JournalRoute"]
    - path: "app/journal/[id]/page.tsx"
      provides: "Server module: generateStaticParams (returns []) + shell"
    - path: "app/journal/[id]/PostDetailClient.tsx"
      provides: "Client component: renders PostDetail with router + MorphingHeader"
      exports: ["PostDetailClient"]
    - path: "app/orders/[id]/page.tsx"
      provides: "Server module: generateStaticParams (returns []) + shell"
    - path: "app/orders/[id]/OrderConfirmationClient.tsx"
      provides: "Client component: renders OrderConfirmation with router"
      exports: ["OrderConfirmationClient"]
  key_links:
    - from: "app/products/[slug]/page.tsx"
      to: "convex deployment"
      via: "generateStaticParams() HTTP fetch to enumerate product _ids at build time"
      pattern: "generateStaticParams"
    - from: "app/journal/page.tsx"
      to: "src/components/MorphingHeader.tsx"
      via: "import MorphingHeader — extracted from App.tsx in Task 1"
      pattern: "MorphingHeader"
    - from: "app/products/[slug]/ProductClient.tsx"
      to: "src/components/ProductPage.tsx"
      via: "productId=slug prop from server component params"
      pattern: "productId.*slug"
---

<objective>
Extract MorphingHeader to its own file (so journal route pages can use it independently of App.tsx), then create all four dynamic route page files: `/products/[slug]`, `/journal`, `/journal/[id]`, `/orders/[id]`.

Purpose: ROUTE-03, ROUTE-04, ROUTE-05, ROUTE-07 — dynamic URL paths for product, journal, and order confirmation pages. Can run in Wave 1 parallel with 03-01 since zero file overlap.
Output: `src/components/MorphingHeader.tsx` (new) + 4 route directories with page.tsx + client component files.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- MorphingHeader lives at src/App.tsx lines 886–1086. Extract to its own file. -->
<!-- Related: useActiveSection (line 842), MorphNavItem (line 983), TIHeaderButton (line 1044) -->
<!-- NAV_ITEMS constant is also in App.tsx — extract it to MorphingHeader.tsx -->

MorphingHeader prop interface (from App.tsx line 886):
```typescript
// src/components/MorphingHeader.tsx (new file)
export function MorphingHeader({
  headerBg, headerBorder, headerShadow,  // any — accepts MotionValue<string> or string literal
  onOpenTI,   // (e: React.MouseEvent) => void
  onOpenCart, // () => void
  onNavTo,    // (target: string) => void
  cartCount,  // number, default 0
  activeOverride, // string | undefined — overrides scroll-based active section
}: { ... })
```

For journal/product routes, pass static string values for headerBg/headerBorder/headerShadow:
- headerBg: "rgba(250,249,246,1)"
- headerBorder: "rgba(201,192,183,0.5)"
- headerShadow: "0 2px 12px -4px rgba(44,24,16,0.10)"

Component prop interfaces:
```typescript
// src/components/ProductPage.tsx
// (props inferred from App.tsx usage at line 668-683)
interface ProductPageProps {
  productId: string;  // product._id (per ROUTE-03)
  onAddToCart: (productId: string, qty: number) => void;
  onOpenCart: () => void;
  cartCount?: number;
}

// src/components/EditorialHub.tsx
// (props inferred from App.tsx usage at line 597-598)
interface EditorialHubProps {
  onProductClick: (id: string) => void;
  onPostOpen: (id: string) => void;
}

// src/components/PostDetail.tsx
interface PostDetailProps {
  postId: string;
  onBack: () => void;
  onProductClick: (productId: string) => void;
}

// src/components/OrderConfirmation.tsx
interface OrderConfirmationProps {
  orderId: string;
  onContinueShopping: () => void;
}
```

Context hooks (same as 03-01):
- useCart(): { cart, addToCart, clearCart, cartCount }
- useCartPanel(): { openCart }
- useProducts(): Product[] | undefined

Path alias: `@/*` → project root. Use `@/src/...` for all src imports.

Convex HTTP endpoint for generateStaticParams:
- URL: process.env.NEXT_PUBLIC_CONVEX_URL + "/api/query"
- Method: POST
- Body: JSON.stringify({ path: "products:list", args: {}, format: "clean_json" })
- Response: { status: "success", value: Product[] } — map to [{ slug: p._id }]
- Wrap in try/catch; return [] on any error so build doesn't fail if Convex is unreachable
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract MorphingHeader to src/components/MorphingHeader.tsx</name>
  <files>src/components/MorphingHeader.tsx</files>
  <action>
Read `src/App.tsx` lines 842–1086. Extract the following to `src/components/MorphingHeader.tsx`:
- `useActiveSection` hook (line 842–885)
- `MorphingHeader` function (line 886–982)
- `MorphNavItem` function (line 983–1043)
- `TIHeaderButton` function (line 1044–1086)
- The `NAV_ITEMS` constant (search for it near line 842 or earlier — likely defined before useActiveSection)

Add `"use client"` as the first line (MorphingHeader uses useState, useScroll, motion).

Imports needed (from App.tsx existing imports):
- React: `useState`
- motion/react: `motion`, `AnimatePresence`, `useScroll`, `useMotionValueEvent`, `useSpring`, `useMotionValue`, type `MotionValue`
- lucide-react: the nav icons used (ShoppingCart, BookOpen, Home, Info, Sparkles, Coffee, etc. — check what icons are referenced inside MorphingHeader/MorphNavItem)
- `asset` from `@/src/lib/asset`

Export `MorphingHeader` and `useActiveSection` as named exports. `MorphNavItem`, `TIHeaderButton`, `NAV_ITEMS` can be non-exported (internal to this file).

Do NOT modify `src/App.tsx` in this task — both files will define MorphingHeader during Wave 1; App.tsx is cleaned up in 03-03.
  </action>
  <verify>
    <automated>Test-Path src/components/MorphingHeader.tsx</automated>
  </verify>
  <done>src/components/MorphingHeader.tsx exists, exports MorphingHeader and useActiveSection, has "use client" directive, TypeScript-valid (no unknown imports).</done>
</task>

<task type="auto">
  <name>Task 2: Create app/products/[slug]/page.tsx and app/products/[slug]/ProductClient.tsx</name>
  <files>app/products/[slug]/page.tsx, app/products/[slug]/ProductClient.tsx</files>
  <action>
Use the server+client pattern for dynamic routes: a server module file exports `generateStaticParams` and a thin async server component default that passes params to a client component.

**app/products/[slug]/page.tsx** (NO "use client" — server module):
```tsx
import { ProductClient } from "./ProductClient";

// ROUTE-03: enumerate all product _ids at build time via Convex HTTP API
export async function generateStaticParams() {
  try {
    const res = await fetch(
      `${process.env.NEXT_PUBLIC_CONVEX_URL}/api/query`,
      {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ path: "products:list", args: {}, format: "clean_json" }),
      }
    );
    if (!res.ok) return [];
    const data = await res.json();
    return (data.value ?? []).map((p: { _id: string }) => ({ slug: p._id }));
  } catch {
    return [];
  }
}

export default async function ProductRoute({
  params,
}: {
  params: Promise<{ slug: string }>;
}) {
  const { slug } = await params;
  return <ProductClient slug={slug} />;
}
```

**app/products/[slug]/ProductClient.tsx** ("use client"):
```tsx
"use client";
import { useRouter } from "next/navigation";
import { ProductPage } from "@/src/components/ProductPage";
import { SiteFooter } from "@/src/components/SiteFooter";
import { useCart } from "@/src/context/CartContext";
import { useCartPanel } from "@/src/context/CartPanelContext";

export function ProductClient({ slug }: { slug: string }) {
  const router = useRouter();
  const { addToCart, cartCount } = useCart();
  const { openCart } = useCartPanel();

  return (
    <div className="min-h-screen bg-natural-bg text-natural-text font-sans flex flex-col">
      <div className="flex-1">
        <ProductPage
          productId={slug}
          onAddToCart={(productId, qty) => { addToCart(productId, qty); openCart(); }}
          onOpenCart={openCart}
          cartCount={cartCount}
        />
      </div>
      <SiteFooter onNavigate={(t) => router.push(t === "home" ? "/" : `/${t}`)} />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>Test-Path "app/products/[slug]/page.tsx"; Test-Path "app/products/[slug]/ProductClient.tsx"</automated>
  </verify>
  <done>Both files exist; page.tsx exports generateStaticParams (async, fetches Convex HTTP API) and a default async server component; ProductClient.tsx is "use client" and renders ProductPage with context hooks.</done>
</task>

<task type="auto">
  <name>Task 3: Create journal and orders/[id] route pages</name>
  <files>
    app/journal/page.tsx,
    app/journal/[id]/page.tsx,
    app/journal/[id]/PostDetailClient.tsx,
    app/orders/[id]/page.tsx,
    app/orders/[id]/OrderConfirmationClient.tsx
  </files>
  <action>
**app/journal/page.tsx** ("use client" — no dynamic params, can be a pure client component):
```tsx
"use client";
import { useRouter } from "next/navigation";
import { EditorialHub } from "@/src/components/EditorialHub";
import { SiteFooter } from "@/src/components/SiteFooter";
import { MorphingHeader } from "@/src/components/MorphingHeader";
import { useCart } from "@/src/context/CartContext";
import { useCartPanel } from "@/src/context/CartPanelContext";

export default function JournalRoute() {
  const router = useRouter();
  const { cartCount } = useCart();
  const { openCart } = useCartPanel();

  return (
    <div className="min-h-screen bg-natural-bg text-natural-text font-sans flex flex-col">
      <MorphingHeader
        headerBg="rgba(250,249,246,1)"
        headerBorder="rgba(201,192,183,0.5)"
        headerShadow="0 2px 12px -4px rgba(44,24,16,0.10)"
        onOpenTI={(e) => { e.stopPropagation(); router.push("/ti"); }}
        onOpenCart={openCart}
        onNavTo={(t) => router.push(t === "home" ? "/" : `/${t}`)}
        cartCount={cartCount}
        activeOverride="editorial"
      />
      <div className="flex-1 pt-20">
        <EditorialHub
          onProductClick={(id) => router.push(`/products/${id}`)}
          onPostOpen={(id) => router.push(`/journal/${id}`)}
        />
      </div>
      <SiteFooter onNavigate={(t) => router.push(t === "home" ? "/" : `/${t}`)} />
    </div>
  );
}
```

**app/journal/[id]/page.tsx** (server module — no "use client"):
Post IDs are not known at build time (CMS-driven via Convex). Return empty array from generateStaticParams; content loads client-side via Convex useQuery.
```tsx
import { PostDetailClient } from "./PostDetailClient";

// ROUTE-05: post IDs unknown at build time — client-side Convex fetch
export async function generateStaticParams() {
  return [];
}

export default async function JournalPostRoute({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;
  return <PostDetailClient id={id} />;
}
```

**app/journal/[id]/PostDetailClient.tsx** ("use client"):
```tsx
"use client";
import { useRouter } from "next/navigation";
import { PostDetail } from "@/src/components/PostDetail";
import { SiteFooter } from "@/src/components/SiteFooter";
import { MorphingHeader } from "@/src/components/MorphingHeader";
import { useCart } from "@/src/context/CartContext";
import { useCartPanel } from "@/src/context/CartPanelContext";

export function PostDetailClient({ id }: { id: string }) {
  const router = useRouter();
  const { cartCount } = useCart();
  const { openCart } = useCartPanel();

  return (
    <div className="min-h-screen bg-natural-bg text-natural-text font-sans flex flex-col">
      <MorphingHeader
        headerBg="rgba(250,249,246,1)"
        headerBorder="rgba(201,192,183,0.5)"
        headerShadow="0 2px 12px -4px rgba(44,24,16,0.10)"
        onOpenTI={(e) => { e.stopPropagation(); router.push("/ti"); }}
        onOpenCart={openCart}
        onNavTo={(t) => router.push(t === "home" ? "/" : `/${t}`)}
        cartCount={cartCount}
        activeOverride="editorial"
      />
      <div className="flex-1 pt-20">
        <PostDetail
          postId={id}
          onBack={() => router.push("/journal")}
          onProductClick={(productId) => router.push(`/products/${productId}`)}
        />
      </div>
      <SiteFooter onNavigate={(t) => router.push(t === "home" ? "/" : `/${t}`)} />
    </div>
  );
}
```

**app/orders/[id]/page.tsx** (server module):
```tsx
import { OrderConfirmationClient } from "./OrderConfirmationClient";

// ROUTE-07: order IDs not known at build time — client-side load via Convex
export async function generateStaticParams() {
  return [];
}

export default async function OrderConfirmationRoute({
  params,
}: {
  params: Promise<{ id: string }>;
}) {
  const { id } = await params;
  return <OrderConfirmationClient id={id} />;
}
```

**app/orders/[id]/OrderConfirmationClient.tsx** ("use client"):
```tsx
"use client";
import { useRouter } from "next/navigation";
import { OrderConfirmation } from "@/src/components/OrderConfirmation";
import { SiteFooter } from "@/src/components/SiteFooter";
import { useCart } from "@/src/context/CartContext";

export function OrderConfirmationClient({ id }: { id: string }) {
  const router = useRouter();
  const { clearCart } = useCart();

  return (
    <div className="min-h-screen bg-natural-bg text-natural-text font-sans flex flex-col">
      <div className="flex-1">
        <OrderConfirmation
          orderId={id}
          onContinueShopping={() => { clearCart(); router.push("/"); }}
        />
      </div>
      <SiteFooter onNavigate={(t) => router.push(t === "home" ? "/" : `/${t}`)} />
    </div>
  );
}
```
  </action>
  <verify>
    <automated>Test-Path app/journal/page.tsx; Test-Path "app/journal/[id]/page.tsx"; Test-Path "app/orders/[id]/page.tsx"</automated>
  </verify>
  <done>All five files exist (journal/page.tsx, journal/[id]/page.tsx + PostDetailClient.tsx, orders/[id]/page.tsx + OrderConfirmationClient.tsx). Each server module exports generateStaticParams; client components use MorphingHeader (from new file) and router-based navigation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build process → Convex HTTP API | generateStaticParams fetches product IDs at build time |
| browser → /products/[slug] | User navigates to a product page with a given _id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering (T) | generateStaticParams HTTP fetch | mitigate | Wrapped in try/catch; returns [] on any error — build continues with no pre-rendered product pages rather than failing |
| T-03-02-02 | Denial of Service (D) | generateStaticParams Convex HTTP fetch | accept | Build-time only; single HTTP call; if Convex is unreachable, build produces an empty product route list — acceptable for CI |
| T-03-02-03 | Tampering (T) | /orders/[id] — orderId from URL | accept | OrderConfirmationClient passes orderId to OrderConfirmation which calls useQuery(api.orders.get, { id }) — Convex validates the ID server-side; an invalid ID returns null and the component shows "not found" |
| T-03-02-04 | Information Disclosure (I) | /journal/[id] — postId from URL | accept | Post content is already public (no auth on EditorialHub); postId is a Convex-generated ID, not sensitive |
</threat_model>

<verification>
- `src/components/MorphingHeader.tsx` exists and exports `MorphingHeader`, `useActiveSection`
- `app/products/[slug]/page.tsx` and `ProductClient.tsx` exist
- `app/journal/page.tsx` exists; uses MorphingHeader from new file
- `app/journal/[id]/page.tsx` + `PostDetailClient.tsx` exist
- `app/orders/[id]/page.tsx` + `OrderConfirmationClient.tsx` exist
- No `src/App.tsx` modifications in this plan
</verification>

<success_criteria>
- MorphingHeader extracted to standalone file; App.tsx still has its own copy (both coexist — App.tsx copy removed in 03-03)
- All four dynamic route directories created with correct server+client split
- generateStaticParams for /products/[slug] calls Convex HTTP API; others return []
- Journal routes include MorphingHeader with static scroll values (not reactive — home page handles reactive in 03-03)
</success_criteria>

<output>
After completion, create `.planning/phases/03-route-migration/03-02-SUMMARY.md`
</output>
