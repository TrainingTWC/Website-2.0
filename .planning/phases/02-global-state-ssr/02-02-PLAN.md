---
phase: 02-global-state-ssr
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/BestsellerCarousel3DLazy.tsx
  - src/components/ProductHero3DLazy.tsx
  - src/components/GalaxySweepLazy.tsx
  - src/components/MagneticCursorLazy.tsx
  - src/components/admin/VisitorMapLazy.tsx
  - src/components/SmoothScroll.tsx
  - src/App.tsx
autonomous: true
requirements:
  - SSR-01
  - SSR-02
  - SSR-03
  - SSR-04

must_haves:
  truths:
    - "next build exits 0 with zero SSR/hydration errors"
    - "BestsellerCarousel3D, ProductHero3D, GalaxySweep load only in the browser"
    - "VisitorMap (Leaflet) does not attempt window access during static export"
    - "MagneticCursor renders only client-side"
    - "SmoothScroll works after adding use client directive"
  artifacts:
    - path: "src/components/BestsellerCarousel3DLazy.tsx"
      provides: "dynamic(ssr:false) re-export of BestsellerCarousel3D"
      exports: ["BestsellerCarousel3D"]
    - path: "src/components/ProductHero3DLazy.tsx"
      provides: "dynamic(ssr:false) re-export of ProductHero3D"
      exports: ["ProductHero3D"]
    - path: "src/components/GalaxySweepLazy.tsx"
      provides: "dynamic(ssr:false) re-export of GalaxySweep"
      exports: ["GalaxySweep"]
    - path: "src/components/MagneticCursorLazy.tsx"
      provides: "dynamic(ssr:false) re-export of MagneticCursor"
      exports: ["MagneticCursor"]
    - path: "src/components/admin/VisitorMapLazy.tsx"
      provides: "dynamic(ssr:false) re-export of VisitorMap"
      exports: ["VisitorMap"]
    - path: "src/components/SmoothScroll.tsx"
      provides: "Lenis wrapper with use client directive"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/BestsellerCarousel3DLazy.tsx"
      via: "import (replaces direct import)"
      pattern: "Lazy"
    - from: "src/App.tsx"
      to: "src/components/SmoothScroll.tsx"
      via: "import (unchanged path, but file now has use client)"
      pattern: "SmoothScroll"
---

<objective>
Create five `*Lazy.tsx` wrapper files that re-export SSR-unsafe components via Next.js `dynamic(ssr: false)`. Add `"use client"` to SmoothScroll.tsx. Update App.tsx to import lazy wrappers. Result: `next build` produces no SSR/hydration errors.

Purpose: The five components use WebGL, DOM APIs, or Leaflet which crash during static export's server-side render phase.
Output: Five new wrapper files, one updated SmoothScroll, App.tsx import updates, passing next build.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-global-state-ssr/02-CONTEXT.md
@.planning/phases/02-global-state-ssr/02-01-SUMMARY.md

<interfaces>
<!-- Export signatures from the original components — lazy wrappers re-export with the same name -->

BestsellerCarousel3D (src/components/BestsellerCarousel3D.tsx):
```typescript
export function BestsellerCarousel3D(props: {
  products: Product[];
  onSelect: (productId: string) => void;
  onAddToCart: (productName: string) => void;
}): JSX.Element
```

ProductHero3D (src/components/ProductHero3D.tsx) — check the file for its export signature.

GalaxySweep (src/components/GalaxySweep.tsx):
```typescript
export function GalaxySweep(props: {
  origin: { x: number; y: number };
  onComplete: () => void;
  duration?: number;
}): JSX.Element
```

MagneticCursor (src/components/MagneticCursor.tsx):
```typescript
export function MagneticCursor(): JSX.Element
```

VisitorMap (src/components/admin/VisitorMap.tsx) — check the file for its export signature.

SmoothScroll (src/components/SmoothScroll.tsx) — currently has NO "use client" directive; uses useEffect only (no module-level window).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lazy wrapper files + add use client to SmoothScroll</name>
  <files>
    src/components/BestsellerCarousel3DLazy.tsx,
    src/components/ProductHero3DLazy.tsx,
    src/components/GalaxySweepLazy.tsx,
    src/components/MagneticCursorLazy.tsx,
    src/components/admin/VisitorMapLazy.tsx,
    src/components/SmoothScroll.tsx
  </files>
  <action>
First read the export signature of `ProductHero3D` and `VisitorMap` before writing their wrappers.

**Pattern for each lazy wrapper** — use this exact structure (named export, same name as original):

```typescript
// src/components/BestsellerCarousel3DLazy.tsx
import dynamic from "next/dynamic";
import type { ComponentProps } from "react";
import type { BestsellerCarousel3D as _T } from "./BestsellerCarousel3D";

export const BestsellerCarousel3D = dynamic(
  () =>
    import("./BestsellerCarousel3D").then((m) => ({
      default: m.BestsellerCarousel3D,
    })),
  { ssr: false }
) as typeof _T;
```

Apply the same pattern for each component:
- `BestsellerCarousel3DLazy.tsx` → re-exports `BestsellerCarousel3D` (import from `"./BestsellerCarousel3D"`)
- `ProductHero3DLazy.tsx` → re-exports `ProductHero3D` (import from `"./ProductHero3D"`)
- `GalaxySweepLazy.tsx` → re-exports `GalaxySweep` (import from `"./GalaxySweep"`)
- `MagneticCursorLazy.tsx` → re-exports `MagneticCursor` (import from `"./MagneticCursor"`)
- `VisitorMapLazy.tsx` (in `src/components/admin/`) → re-exports `VisitorMap` (import from `"./VisitorMap"`)

**SmoothScroll.tsx**: Add `"use client";` as the very first line of the file (before all imports). Do not modify any other code in the file.
  </action>
  <verify>
    <automated>cd c:\Users\TWC\Downloads\brewmatch-ai && (Test-Path src/components/BestsellerCarousel3DLazy.tsx) -and (Test-Path src/components/GalaxySweepLazy.tsx) -and (Get-Content src/components/SmoothScroll.tsx | Select-Object -First 1)</automated>
  </verify>
  <done>All five *Lazy.tsx files exist. SmoothScroll.tsx first line is `"use client";`. No TypeScript errors in new files.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx imports to use lazy wrappers + verify build</name>
  <files>src/App.tsx</files>
  <action>
In `src/App.tsx`, update the component imports to point to the lazy wrappers. Only the import paths change — no JSX or logic changes.

Find and replace each import:

1. `import { BestsellerCarousel3D } from "./components/BestsellerCarousel3D"`
   → `import { BestsellerCarousel3D } from "./components/BestsellerCarousel3DLazy"`

2. `import { ProductHero3D } from "./components/ProductHero3D"` (or wherever it's imported)
   → `import { ProductHero3D } from "./components/ProductHero3DLazy"`

3. `import { GalaxySweep } from "./components/GalaxySweep"`
   → `import { GalaxySweep } from "./components/GalaxySweepLazy"`

4. `import { MagneticCursor } from "./components/MagneticCursor"`
   → `import { MagneticCursor } from "./components/MagneticCursorLazy"`

For VisitorMap — it is imported inside `src/components/admin/VisitorMap.tsx` directly or in an admin component. Check whether `VisitorMap` is imported anywhere in App.tsx; if not (it's only used within the admin shell), find where it IS imported and update that file's import to use `VisitorMapLazy` instead.

After updating imports, run `npm run build` and verify it exits 0. If there are TypeScript errors related to the lazy wrapper typing (`as typeof _T` approach), replace with a simpler pattern:
```typescript
export const BestsellerCarousel3D = dynamic(
  () => import("./BestsellerCarousel3D").then((m) => ({ default: m.BestsellerCarousel3D })),
  { ssr: false }
);
```
(Without the type cast — TypeScript will infer it as `ComponentType<{}>` but the build will still pass with `typescript.ignoreBuildErrors: true`.)
  </action>
  <verify>
    <automated>cd c:\Users\TWC\Downloads\brewmatch-ai && npm run build 2>&1 | Select-String "Route|error Error" | Select-Object -Last 10</automated>
  </verify>
  <done>`next build` exits 0. App.tsx imports point to *Lazy files for all five SSR-unsafe components. The `./out` directory is produced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → dynamic import | Lazy component loaded only in browser — no server execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Denial of Service | dynamic() loading | accept | If lazy chunk fails to load (network error), Next.js renders `null` (no loading fallback specified). Acceptable — these are progressive enhancement components (3D carousel, cursor). Core shop functionality unaffected. |
| T-02-06 | Information Disclosure | Lazy chunk URLs | accept | Chunk filenames are hashed by Next.js — no meaningful information exposure. |
</threat_model>

<verification>
1. `npm run build` (next build) exits 0
2. `Get-Content src/components/SmoothScroll.tsx | Select-Object -First 1` → `"use client";`
3. `Select-String -Path src/App.tsx -Pattern "BestsellerCarousel3DLazy|GalaxySweepLazy|MagneticCursorLazy"` → matches found
4. `Select-String -Path src/App.tsx -Pattern "from.*BestsellerCarousel3D[^L]"` → no matches (old direct import gone)
5. `Test-Path ./out/index.html` → True
</verification>

<success_criteria>
- Five *Lazy.tsx wrapper files created
- SmoothScroll.tsx has "use client" as first line
- App.tsx imports all five SSR-unsafe components from their Lazy wrappers
- next build exits 0, ./out produced
- No hydration warnings in next build output
</success_criteria>

<output>
After completion, create `.planning/phases/02-global-state-ssr/02-02-SUMMARY.md`
</output>
