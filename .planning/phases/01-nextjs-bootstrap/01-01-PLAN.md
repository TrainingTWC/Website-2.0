---
phase: 01-nextjs-bootstrap
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - postcss.config.mjs
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/vite-env.d.ts
autonomous: true
requirements:
  - MIG-01
  - MIG-02
  - MIG-03

must_haves:
  truths:
    - "package.json scripts use next dev / next build / next start"
    - "next.config.ts exists with output: 'export', trailingSlash: true, images.unoptimized: true"
    - "postcss.config.mjs configures @tailwindcss/postcss (Tailwind v4)"
    - "tsconfig.json is valid for Next.js 15 App Router"
    - "Vite config, Vite entry HTML, and Vite-specific src/ files are deleted"
  artifacts:
    - path: "next.config.ts"
      provides: "Next.js build configuration with static export"
      contains: "output: \"export\""
    - path: "postcss.config.mjs"
      provides: "Tailwind v4 PostCSS integration"
      contains: "@tailwindcss/postcss"
    - path: "tsconfig.json"
      provides: "TypeScript config for Next.js App Router"
      contains: "\"preserve\""
  key_links:
    - from: "postcss.config.mjs"
      to: "src/index.css"
      via: "@tailwindcss/postcss processes @import 'tailwindcss' directive"
      pattern: "@tailwindcss/postcss"
    - from: "next.config.ts"
      to: "./out directory"
      via: "output: 'export' directs Next.js to write static files to ./out"
      pattern: "output.*export"
---

<objective>
Replace the Vite build pipeline with Next.js 15 App Router. Update package.json, install Next.js 15 and PostCSS Tailwind adapter, configure next.config.ts for static export, and delete all Vite-specific files.

Purpose: MIG-01/02/03 — establish the build foundation before the app shell is wired in Plan 02.
Output: A repository that has Next.js 15 installed with correct static-export config and Tailwind v4 via PostCSS. Vite is gone. `npm run build` will fail until Plan 02 creates `app/` directory — that is expected.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Current package.json scripts (to be replaced) -->
Current scripts:
  "dev": "vite --port=3000 --host=0.0.0.0"
  "build": "vite build"
  "preview": "vite preview"
  "clean": "rm -rf dist"
  "lint": "tsc --noEmit"

Current Vite deps to REMOVE:
  "@tailwindcss/vite": "^4.1.14"
  "@vitejs/plugin-react": "^5.0.4"
  "vite": "^6.2.3"  (appears in both dependencies AND devDependencies)

Deps to KEEP (unchanged):
  "@auth/core", "@convex-dev/auth", "@react-three/drei", "@react-three/fiber",
  "@types/leaflet", "convex", "get-shit-done-cc", "leaflet", "lenis",
  "lucide-react", "motion", "react", "react-dom", "react-leaflet",
  "react-markdown", "three"
  devDeps: "@types/node", "@types/three", "autoprefixer", "sharp", "tsx", "typescript"

Current tailwindcss version: "^4.1.14" — keep this version

Current tsconfig.json jsx: "react-jsx" — must change to "preserve"
Current tsconfig.json experimentalDecorators: true — REMOVE (only needed in convex/tsconfig.json)
Current tsconfig.json allowImportingTsExtensions: true — REMOVE (Next.js does not need it)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update package.json — swap Vite for Next.js 15</name>
  <read_first>
    - package.json (current dependencies and scripts — read before editing)
  </read_first>
  <files>package.json</files>
  <action>
Rewrite package.json with these exact changes:

**scripts** (replace all):
```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "clean": "Remove-Item -Recurse -Force .next,out -ErrorAction SilentlyContinue",
  "lint": "tsc --noEmit"
}
```

**dependencies** — ADD:
```
"next": "^15.3.2"
"@tailwindcss/postcss": "^4.1.14"
"postcss": "^8.5.3"
```

**dependencies** — REMOVE:
```
"@tailwindcss/vite"
"@vitejs/plugin-react"
"vite"
```

**devDependencies** — REMOVE:
```
"vite"
```

After editing package.json, run:
```
npm install
```

This installs Next.js 15, @tailwindcss/postcss, postcss; removes Vite packages.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); console.log(p.dependencies.next ? 'NEXT_OK' : 'MISSING_NEXT'); console.log(!p.dependencies.vite ? 'VITE_REMOVED' : 'VITE_STILL_PRESENT')"</automated>
  </verify>
  <done>package.json has next ^15.x in dependencies, no vite entry, scripts use "next dev" and "next build". npm install completed without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create next.config.ts, postcss.config.mjs, update tsconfig.json</name>
  <read_first>
    - tsconfig.json (current settings — read before editing)
    - src/index.css (verify @import "tailwindcss" directive is present — PostCSS must process it)
  </read_first>
  <files>next.config.ts, postcss.config.mjs, tsconfig.json</files>
  <action>
**Create next.config.ts** (new file at repo root):
```typescript
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  trailingSlash: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
```

**Create postcss.config.mjs** (new file at repo root):
```javascript
/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

**Rewrite tsconfig.json** (replace entire file):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "dom", "dom.iterable"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": false,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "jsx": "preserve",
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

Key changes from original:
- `jsx`: "react-jsx" → "preserve" (required by Next.js App Router)
- Removed `experimentalDecorators`, `useDefineForClassFields` (live in convex/tsconfig.json)
- Removed `allowImportingTsExtensions` (not needed for Next.js)
- Added `"plugins": [{ "name": "next" }]`, `"incremental": true`
- Added `"include"` and `"exclude"` arrays
- Added `esModuleInterop`, `resolveJsonModule`
- `strict: false` — the existing codebase is not written with strict TypeScript
  </action>
  <verify>
    <automated>node -e "const c=require('./next.config.ts'); console.log('next.config exists')" 2>/dev/null || node -e "const fs=require('fs'); const c=fs.readFileSync('next.config.ts','utf8'); console.log(c.includes('output: \"export\"') ? 'OUTPUT_EXPORT_OK' : 'MISSING_OUTPUT'); console.log(c.includes('trailingSlash: true') ? 'TRAILING_SLASH_OK' : 'MISSING_TRAILING_SLASH'); console.log(c.includes('unoptimized: true') ? 'IMAGES_OK' : 'MISSING_IMAGES')"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts contains `output: "export"`
    - next.config.ts contains `trailingSlash: true`
    - next.config.ts contains `unoptimized: true`
    - postcss.config.mjs contains `"@tailwindcss/postcss": {}`
    - tsconfig.json contains `"jsx": "preserve"`
    - tsconfig.json contains `"name": "next"` (plugins entry)
    - tsconfig.json does NOT contain `"allowImportingTsExtensions"`
  </acceptance_criteria>
  <done>All three config files created/updated. next.config.ts has static export settings. postcss.config.mjs uses @tailwindcss/postcss. tsconfig.json uses jsx: "preserve" with Next.js plugin.</done>
</task>

<task type="auto">
  <name>Task 3: Delete Vite artifacts (vite.config.ts, index.html, src/main.tsx, src/vite-env.d.ts)</name>
  <read_first>
    - index.html (extract title, description, icon, viewport meta — these must move to app/layout.tsx in Plan 02)
    - src/main.tsx (confirm it only creates React root — no logic to preserve)
    - src/vite-env.d.ts (confirm it only has vite/client reference)
  </read_first>
  <files>vite.config.ts, index.html, src/main.tsx, src/vite-env.d.ts</files>
  <action>
Read index.html first and record the following values for Plan 02 (app/layout.tsx will need them):
- title: "Third Wave Coffee"
- description: "Discover your perfect coffee match with AI-powered recommendations. Premium single-origin beans, essential gear, and curated bundles."
- icon: "/favicon.png"
- viewport: `width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no`
- preconnect: `https://different-bulldog-772.convex.cloud`

Then delete:
```
rm vite.config.ts
rm index.html
rm src/main.tsx
rm src/vite-env.d.ts
```

On Windows PowerShell use:
```powershell
Remove-Item vite.config.ts, index.html, src/main.tsx, src/vite-env.d.ts
```

These files are replaced by:
- vite.config.ts → next.config.ts (done in Task 2)
- index.html → app/layout.tsx (done in Plan 02)
- src/main.tsx → app/layout.tsx + app/page.tsx (done in Plan 02)
- src/vite-env.d.ts → next-env.d.ts (auto-generated by Next.js)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['vite.config.ts','index.html','src/main.tsx','src/vite-env.d.ts'].forEach(f => { try { fs.accessSync(f); console.log('STILL_EXISTS: '+f); } catch { console.log('DELETED_OK: '+f); } })"</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.ts` does NOT exist (file not found)
    - `index.html` does NOT exist (file not found)
    - `src/main.tsx` does NOT exist (file not found)
    - `src/vite-env.d.ts` does NOT exist (file not found)
  </acceptance_criteria>
  <done>All four Vite-specific files deleted. No remaining references to Vite entry points. Values from index.html noted for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json → npm registry | Dependency resolution from external registry |
| next.config.ts → build output | Config controls what is included in the static export |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | package.json | mitigate | Pin exact versions with `npm install --save-exact` or lock file; verify `package-lock.json` is committed |
| T-01-02 | Information Disclosure | next.config.ts `output: 'export'` | accept | Static export has no server-side secrets; all client assets are intentionally public |
| T-01-03 | Elevation of Privilege | npm install (postinstall scripts) | accept | All packages are established libraries (Next.js, Tailwind); no unknown packages added |
</threat_model>

<verification>
After completing all 3 tasks:

```bash
# Verify Next.js is installed
node -e "require('next/package.json')" && echo "NEXT_INSTALLED"

# Verify all Vite artifacts gone
node -e "const fs=require('fs'); const gone=['vite.config.ts','index.html','src/main.tsx','src/vite-env.d.ts']; gone.every(f=>{try{fs.accessSync(f);return false}catch{return true}}) ? console.log('ALL_VITE_DELETED') : console.log('SOME_VITE_REMAIN')"

# Verify next.config.ts has static export
node -e "const fs=require('fs'); const c=fs.readFileSync('next.config.ts','utf8'); ['output: \"export\"','trailingSlash: true','unoptimized: true'].every(s=>c.includes(s)) ? console.log('CONFIG_OK') : console.log('CONFIG_INCOMPLETE')"
```

NOTE: `npm run build` will fail after this plan — that is EXPECTED. Plan 02 creates the `app/` directory that Next.js requires. Do not attempt to run `next build` until Plan 02 completes.
</verification>

<success_criteria>
- next.config.ts exists with `output: "export"`, `trailingSlash: true`, `images: { unoptimized: true }`
- postcss.config.mjs exists with `@tailwindcss/postcss` plugin
- tsconfig.json uses `jsx: "preserve"`, has `plugins: [{ "name": "next" }]`, no `allowImportingTsExtensions`
- package.json scripts: `dev: next dev`, `build: next build`, `start: next start`
- next.js packages installed in node_modules (`next`, `@tailwindcss/postcss`, `postcss`)
- Vite files deleted: `vite.config.ts`, `index.html`, `src/main.tsx`, `src/vite-env.d.ts`
</success_criteria>

<output>
After completion, create `.planning/phases/01-nextjs-bootstrap/01-01-SUMMARY.md`
</output>
