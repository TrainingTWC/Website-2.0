# Plan v6.0-phase-01-06 — Manual UAT Checkpoint

## What shipped

- `UAT.md` — comprehensive 9-section manual verification checklist covering:
  1. Pre-flight build / route smoke
  2. Adaptive perf tier classification (high/mid/low + reduced-motion)
  3. Web vitals telemetry into Convex `webVitals`
  4. SmoothScroll + Lenis tier gating + D-05 fix verification
  5. Cinematic single-`useScroll` + IO `willChange` + tier static fallback
  6. LazyMotion + Convex preconnect + SmartImage `sizes`
  7. Heavy-component tier gating (3D Canvas, MagneticCursor, GalaxySweep, BestsellerCarousel)
  8. End-to-end user flows (home → checkout, journal, admin)
  9. Lighthouse + scroll-FPS + INP targets

## Gate status

- This plan is `gate: blocking`, `checkpoint: human-verify`.
- User was unavailable for live sign-off; phase advanced through code-complete only.
- **STATE.md and ROADMAP.md are NOT yet flipped to phase-complete.** They will move to `complete` only after the UAT checklist in `UAT.md` is signed off by a human reviewer.

## Next action for the user

1. Run through `UAT.md` against a production build of master (HEAD ≥ commit `3730f24`).
2. Fill the sign-off table at the bottom.
3. Once all items pass, manually update:
   - `.planning/STATE.md` — frontmatter `phases_done: 1`, `plans_done: 6`, status `complete`; body sections "Current Position" + "Completed Phases (v6.0)".
   - `.planning/ROADMAP.md` — mark phase 1 complete.
4. Commit with `chore(v6.0-01): UAT signed off, phase complete`.

## Why we did not auto-complete

Honoring the blocking gate. Marking a phase complete without human verification would defeat the safety contract of this checkpoint and risk shipping perf regressions undetected (Lighthouse, FPS, INP targets cannot be self-verified by the build pipeline).
